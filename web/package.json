{
  "name": "pathcal-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "data:update": "node scripts/update-terms.mjs",
    "bookmarklet:build": "node scripts/build-bookmarklet.mjs",
    "predev": "npm run bookmarklet:build",
    "dev": "vite",
    "prebuild": "npm run data:update && npm run bookmarklet:build",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "cheerio": "^1.1.2",
    "terser": "^5.43.1",
    "vite": "^7.1.3"
  }
}
